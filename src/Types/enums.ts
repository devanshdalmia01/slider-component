export enum SLIDER_TYPE {
    CONTINUOUS,
    DISCRETE,
}

export enum VALUE_TYPE {
    SINGLE,
    RANGE,
}

export enum HANDLE_SIZE {
    SIZE_24 = 24,
    SIZE_32 = 32,
}

export enum DOT_WIDTH {
    WIDTH_4 = 4,
}
